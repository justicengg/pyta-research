# UI Prototype Note

These files are design references and prototypes only.

They are committed to support collaboration and future frontend integration, but they are **not** wired into the current Layer 2 implementation round.

Current implementation priority:
1. Layer 2 secondary-market backend chain
2. schema / orchestrator / report contracts
3. frontend integration later, after the chain is stable
